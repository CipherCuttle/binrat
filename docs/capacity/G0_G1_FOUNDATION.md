# BINRAT G0/G1 — frozen foundation and staging request

**Date:** 2026-09-25. **Status:** isolated engineering only. This file does not grant merge, production, staging spend, public checkout or token authority.

## Exact-head dependency freeze

- This G0/G1 branch starts from research PR #35 at `54825038129d9bf4c9730664919cb7e83dd74211`, itself stacked on PR #32 `f1411ab83eddb25ed18c88584b6e26e75e86f163`. No rebase/merge is authorized.
- Independent visual PR #31 `0294888b78c715b02aaeef269edda53a0a4888a5`: current design contract is `docs/design/BENTO_DASHBOARD_V1.md`, visually unapproved. Do not copy its old 5042 holder wording over current owner-selected 4663-only token policy.
- Independent Radar falsification PR #33 `4c5695951998430f1fa57dcc341a157cb8c375cb` is synthetic research, not observed trading alpha.
- Source contract: `docs/research/SCALABLE_LAUNCH_10K_V1.md`. Machine contract: `bench/capacity-profile.v1.json`. Deviations require reviewed changes, not benchmark-runner tweaks.

## Source inventory and current-versus-planned truth

Inspected: `cloudflare/wrangler.example.jsonc`, `cloudflare/schema.sql`, `src/cloudflare/{worker,syncQueue,d1Store,ratWatch,ratRadarStore,runtimeState,d1Types}.ts`, existing CI and Cloudflare workflow gates, frontend PR roadmap and capability manifest. One Worker combines `/health`, `/api/{health,feed,capabilities,dumpster-ledger,creator/:address,bag/:id,bag/:id/replay,rat-radar/watchlist,rat-radar/activity/:id,rat-radar/address/:address/activity}`, holder candidate/login handling and `POST /telegram/webhook`. The Pons candidate API is deliberately disabled without a test-only dependency. Neither candidate reads nor candidate login constitute Robinhood launch ingestion.

Example deployment: one `DB` D1 binding, one `SYNC_QUEUE` producer/consumer, one-minute Cron, consumer batch 1/concurrency 1/max retries 5. Shared `SYNC_CYCLE`, `OBSERVATION_CYCLE`, `RAT_WATCH_CYCLE`, `RAT_RADAR_CYCLE` create potential head-of-line blocking. Launches, checkpoints and Radar receipts carry `chain_id`, but legacy Watch subscriptions key by `(chat_id,creator)` and alerts by `(chat_id,launch_id)`: 4663 Watch requires an additive, read-compatible migration later, not a silent rewrite now. This branch adds no tables and rewrites no evidence IDs, hashes or receipt DTOs.

Actual public `/api/feed` currently reconstructs a D1 projection for each request and API responses are `Cache-Control: no-store`. There is NO measured edge-cache hit ratio; the 70% cached target describes a future cache/projection layer. Before a full benchmark can pass, add versioned immutable per-chain cached snapshots and observable cache hit/miss markers under separate integration authority. Never count quick 503/429/404s as successful latency. Separate source coverage and freshness for 5042/4663; the latter currently has no verified indexer.

Existing CI: `pnpm check` compiles TypeScript, runs all `test/*.test.ts`, and checks legacy web. PR #35 parent exact-head CI passed 224 tests, **not** a load test. Cloudflare dry-run is automatic for Worker source changes; GitHub Actions deployment is constrained to separate deployment branches/conditions. GitHub secret **names** referenced by deployment include `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `ARC_RPC_URL`; runtime references `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET` and holder/candidate feature vars. Their existence, current values, billing contract, current deployed Worker version and current D1 usage are NOT established by reading workflow definitions. The capability manifest contains historical 2026-09-24 deployment assertions, not current live readback. Never copy credential values into logs, commits or PRs.

## Benchmark, telemetry and authority boundaries

`bench/capacity-profile.v1.json` declares 10K DAU, 1K open sessions, 100 mixed RPS × 30m (180K), 300 RPS × 5m (90K), 70/15/10/5 route weights, separate 0/50/90/99% cache profiles and gates. The offline virtual-time simulator and CI smoke validate **workload shape, deterministic fanout scheduling, chaos invariants and modeled queue pressure** only. Virtual p95 is not a measured Worker p95. `bench/k6-capacity.js` is a REAL HTTP isolated-staging driver, hard-disabled without explicit environment approval; its full contract cannot pass until cache, 4663 ingestion, accounts and premium endpoints genuinely exist. A cold one-lane counterexample must fail the modeled latency gate before any architectural optimization is claimed.

Instrumentation is opt-in `BINRAT_CAPACITY_TELEMETRY_ENABLED=true`: event schema `binrat.capacity.telemetry/1` with route **templates only** (no addresses, message content or tokens), 5042 chain marker for existing Arc work, response status, latency, returned D1 rows, provider-reported D1 rows-read/written when supplied, SQL-operation counts, unknown-scan counter, queue wait/ack/retry and cache response directive. Unknown Cloudflare billing metrics are **null**, not invented. Optional-work guard remains inactive unless `BINRAT_CAPACITY_GUARDS_ENABLED=true` AND `BINRAT_OPTIONAL_WORK_MODE=SHED_OPTIONAL`. It only rejects explicitly classified replay/deep-Radar operations with a truthful 503 and Retry-After; it is NOT a global dollar cap or a substitute for Cloudflare WAF and durable account quotas. Normal public receipts and chain source state are never rewritten by metering.

Rollback invariant: unset both flags to restore the original dispatch behavior. Reverting this isolated branch changes no existing D1 schema or source hashes. Telemetry requires a separate operator decision, measured log volume and privacy review before any deploy; no auto activation from this PR.

## Chaos and finance gates

Synthetic tests must include RPC 429/timeout, D1 failed write, Telegram duplicate/429/drain, replayed/forged billing event, refund, 1K contended quota attempts and immutable synthetic block/hash conflict. These fakes are NOT production provider adapters. Preserve source-specific fail-closed behavior under provider failure; consumer retry/DLQ limitations of the currently shared queue are an explicitly open G2/G3 risk. Dedicated alert queue, chain-scoped Watch migration, payment durability and real 4663 receipts belong to the later separately authorized lanes.

**Spend request — NOT APPROVED:** choose an all-in monthly spending limit and a separate one-time isolated Paid Workers staging load budget; confirm actual account plan, D1 reads/writes, archive RPC contract and WAF capability before spending. One 100 RPS × 30m run invokes 180K Workers calls, above the quoted 100K/day Free request allocation, excluding other traffic. Proposed acceptance sequence is free CI mocks → explicitly funded isolated 10 RPS smoke → 50 RPS ramp → 100 RPS × 30m → 300 RPS × 5m. Stop on measured >1% system errors, failed success-only latency, overloaded D1, unbounded queue or cost ceiling; require operator-readback and controlled rollback. Cloudflare spend alerts alone cannot hard-cap adversarial inbound invocation charges.

At **zero paid conversion and zero token fees**, recurring revenue is $0. Planning-only monthly cost bands: **$5 Cloudflare Paid minimum plus measured D1/RPC/observability**, or separately stress **$1,200 / $2,000 / $5,000** full operating assumptions; none is an observed BINRAT invoice or includes legal/security/founder time. At hypothetical $29 VAT-inclusive Pro, 25% VAT (if applicable), 8% processor/refund haircut and $4/seat variable cost, net contribution is $16.88; fixed $2,000/month requires 119 subscribers to break even, 158 for the plan's 25% cushion. Price, VAT treatment and conversion remain unverified. Never capitalize token secondary-market purchases as operating revenue.

## Following lanes — separate PRs, NOT G1 success claims

**A**: independently verify actual 4663 Pons V2 factory/emitter/event epoch against real receipts; add per-chain checkpoint/reorg and 3 genuine launch+curve replay fixtures, phase-aware Radar, truthful post-graduation PARTIAL until independently verified V4, no cross-chain human identity inference.

**B**: add a durable independent account/entitlement store, bounded prepaid quota reserves, idempotent signed checkout webhook with refund/chargeback proof, release feature flag OFF, actual per-chain cost allocation, and later approved hosted fiat provider. Holder bonus OFF until real deployed 4663 token, verified wallet/contract wallet, threshold, compliance and explicit authority. No new subscriptions should create liabilities above reserves.

**External gates:** full Paid staging budget + credentials, read-only production metrics, current on-chain verification, visual owner sign-off and independent legal/security/merchant roles. No merges, D1 writes, deployment, token action or public checkout under this PR.
