# G0.1 — OWNER VISUAL REVIEW. No HomeScene integration yet.

Date: 2026-09-24. Same draft PR #31 only. This is the owner-approved hostile-review **repair experiment**, NOT North Star visual approval and NOT a production preview.

## New source-derived visual deliverables

Reproducible review script `scripts/north-star/build-g01.py` reads exactly the approved master rat, world, and Home PNG Git blobs. The already verified G0 foreground segmentation is reused if present. It produces five **individual** consistently sized, physically illustrated, genuinely empty portal images (600×270; lossless WebP) plus separate 1672×941 desktop and 390×844 mobile first-view compositions. A full-scroll 390×1442 mobile study and contact sheet are retained as Actions artifacts.

The five portal illustrations encode actual product semantics without misleading depictions: Radar uses an empty CRT sweep rather than fabricated geographic activity; Replay uses four empty time-horizon frames rather than CCTV people; Ledger uses a blank, sealed paper ledger rather than fake money flows; Creator Files displays empty address slots, never a human portrait; Watch depicts a disconnected Telegram handoff switch, never fabricated delivered alerts. All interactive title, copy, routes, checkpoint, DEMO/LIVE status, availability and coverage **must remain HTML/API-driven** when integrating.

## Required approval observations

**Desktop:** correct unredesigned viewer-right red eye, complete foreground, horizontal masthead, monumental cream headline, working yellow Dumpster target as next engineering batch, first-view five illustrated equally weighted portals, cohesive identity band, honest status entry without invented data.

**Mobile:** keep the tested **4-tab M1 nav** (Discover / Radar / Saved / More) and existing routes; first view must show headline, explanation, CTA, genuine status surface and one featured Radar doorway. The other four portals remain accessible via purposeful vertical discovery. Never make fake sign-in, pricing, wallet or token-launch controls.

**Current limitations:** mockups are flat PIL visual-review compositions, not browser captures. Individual illustrations are newly authored physical UI decor informed by the exact approved art; do not label their procedural shapes as extracted approved original art. Transparent rat cut uses the exact G0 source-backed mask where available, but hidden pixels remain unrecoverable. Current G0.1 compositor includes static example prose and placeholders only for layout; no displayed counts, mock hashes, human identities or claimed Telegram subscriptions.

## Next actual engineering batch, only AFTER owner approves revised composition

Create an isolated HomeScene, real horizontal site masthead and interactive five-portal rail on desktop '/', with scoped CSS. Retain forensic ShellNav/StatusRail for interiors; preserve real DEMO/LIVE, API/coverage and mobile M1 routes. Functional and visual acceptance are separate. No GitHack publish, merge, production deploy, token, Pons, wallet or fund authority.

WCAG 2.2 AA acceptance target: body text contrast >= 4.5:1, large display >= 3:1; target >= 24px minimum and design preference >= 44px; keyboard visible focus and unobscured controls. Core Web Vitals measurement follows only after browser integration: LCP <= 2.5s, CLS <= 0.1, INP <= 200ms at the 75th percentile of real users.
